#include <bits/stdc++.h>

using namespace std;
using vi = vector<int>;

void solve() {
    int n, k;
    cin >> n >> k;
    // Your code here
    vector<pair<int,int>> a(n);
    for(int i=0;i<n;i++) {
        cin>>a[i].first;
        cin>>a[i].second;
    }

    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

   solve();
}